# Postgres credentials (lab only — never reuse these for anything real)
POSTGRES_USER=dga
POSTGRES_PASSWORD=dga
POSTGRES_DB=dataguard

# Host port (55432 avoids clashing with any local Postgres install)
DB_PORT=55432
ADMINER_PORT=8080
