# Postgres credentials (lab only — never reuse these for anything real)
POSTGRES_USER=dga
POSTGRES_PASSWORD=dga
POSTGRES_DB=dataguard

# Host ports (change if these clash with something local)
DB_PORT=55432
ADMINER_PORT=8080
