{
  "name": "polymarket-local",
  "version": "1.0.0",
  "description": "Local Node.js server for Polymarket algo lab — bypasses browser CORS, proxies API calls server-side, relays Binance WebSocket feed.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.7.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
